============
Installation
============

At the command line::

    $ easy_install django-filer-celery

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-filer-celery
    $ pip install django-filer-celery
